
[tool.ruff.lint]
# flake8-bugbear (B)
# flake8-comprehensions (C4)
# pycodestyle errors (E)
# Pyfakes (F)
# flake8-implicit-str-concat (ISC)
# flake8-use-pathlib (PTH)
# flake8-bandit (S)
# flake8-simplify (SIM)
# Warning (W)
select = ["B", "C4", "E", "F", "ISC", "PTH", "S", "SIM", "W"]
# select = ["ALL"]
